/**
 * Shared and feature UI components live under `components/`.
 * Replace this file when you add real components; it exists only to reserve the folder.
 */
export function Placeholder(): null {
  return null
}
